SHELL CONFIGURATION
File: kernel/service/shell/config.asm
Author: arfy slowy

CONSTANT

SERVICE_SHELL_CACHE_SIZE_byte:
  Size of the shell input buffer (in bytes).
  Calculated as: KERNEL_VIDEO_WIDTH_char - prompt_display_length - 1
  Ensures input never exceeds one line on the display.
